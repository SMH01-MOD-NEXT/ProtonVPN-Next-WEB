{
  "name": "protonvpn-next-website",
  "private": true,
  "version": "2.0.0",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test \"tests/**/*.test.js\"",
    "event": "node scripts/event-bypass.js",
    "deploy": "npm run build && wrangler deploy",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@types/node": "^24.0.0",
    "tailwindcss": "^4.0.0",
    "vite": "^8.0.0"
  }
}
